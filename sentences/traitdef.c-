
trait Printable:

    dec __display__(Self self) returns Str

class Coord contains (
    Float x,
    Float y,
):
    trait Printable:

        def __display__(Coord self) returns Str:
            return "<Coord x:"+str(self.x)+" y:"+str(self.y)+">"
