def addints(Int a, Int b) returns Int:
    return 5

def addstr(Str a, Str b) returns Str:
    baz = 0
    baz = "baz"
    return baz

def bar(Intable foo) returns <Int>: ##this returns and array of ints##
    ###
    this is a multi -line comment
    ###
    return <int(foo)>
